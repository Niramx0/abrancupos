version: '3.8'
services:
  db:
    image: mysql:8.0 # Or any other desired version like mysql:5.7
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: your_root_password # Replace with a strong password
      MYSQL_DATABASE: proyecto_ips # Optional: Create a database on startup
      MYSQL_USER: your_user # Optional: Create a user on startup
      MYSQL_PASSWORD: your_user_password # Optional: Set password for the user
    ports:
      - "3306:3306" # Maps host port 3306 to container port 3306
    volumes:
      - db_data:/var/lib/mysql # Persistent storage for database data
      # - ./init_scripts:/docker-entrypoint-initdb.d # Optional: For custom SQL scripts
    restart: unless-stopped # Automatically restart the container unless manually stopped

volumes:
  db_data: # Define the named volume for persistent data
